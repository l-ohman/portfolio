{
  "homepage": "https://l-ohman.com",
  "name": "lohman-portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build",
    "deploy": "gh-pages -d build",
    "fix-paths": "python3 deploy.py",
    "build-and-deploy": "npm run build && npm run fix-paths && npm run deploy"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^4.6.0",
    "react-scripts": "^2.1.3"
  },
  "devDependencies": {
    "styled-components": "^5.3.6"
  }
}
